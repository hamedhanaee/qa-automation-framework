[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qa-api-automation-framework"
version = "0.1.0"
description = "Pytest-based API automation framework"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.23,<5.0",
    "playwright>=1.55,<2.0",
    "pytest>=8.0,<9.0",
    "pytest-html>=4.1,<5.0",
    "pytest-playwright>=0.7,<1.0",
    "requests>=2.32,<3.0",
    "ruff>=0.12,<1.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
log_level = "INFO"
markers = [
    "regression: complete API and UI behavior coverage",
    "smoke: fast checks for service availability and core user workflows",
    "ui: browser-based user interface tests",
]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
